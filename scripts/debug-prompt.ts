#!/usr/bin/env tsx
/**
 * Thin wrapper for backward compatibility.
 * Use `npm run test:llm generate "prompt"` for the full CLI tool.
 */
// biome-ignore lint/suspicious/noConsole: CLI wrapper
console.log('ℹ️  Use `npm run test:llm generate "prompt"` instead.')
